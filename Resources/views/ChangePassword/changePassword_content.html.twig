{% form_theme changePasswordForm theme %}

<div class="user-box">
<form action="{{ path('fos_user_change_password') }}" {{ form_enctype(changePasswordForm) }} method="POST" class="fos_user_change_password">
    {{ form_widget(changePasswordForm) }}
    <div>
        <input class="button" type="submit" value="{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
    <div>&nbsp;</div>
</form>
</div>
